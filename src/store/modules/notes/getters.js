export default {
  notes(state) {
    return state.notes
  },

  totalItems(state) {
    return state.totalItems
  },

  page(state) {
    return state.page
  },

  singleNote(state) {
    return state.singleNote
  },

  loadingNotes(state) {
    return state.loadingNotes
  }
}
